{
  "name": "jobhackai-app",
  "version": "1.0.0",
  "description": "JobHackAI Cloudflare Application",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "cp -f public/_redirects out/_redirects",
    "start": "next start",
    "export": "next build && next export",
    "deploy": "wrangler pages deploy out",
    "deploy:qa": "wrangler pages deploy out --env qa",
    "deploy:prod": "wrangler pages deploy out --env production",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase": "^10.7.0",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "eslint": "^8.56.0",
    "eslint-config-next": "^14.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
